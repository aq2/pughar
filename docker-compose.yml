web:
  image: nginx:latest
  ports:
    - "3000:80"
  volumes:
    - ./www:/www
    - ./site.conf:/etc/nginx/conf.d/default.conf
  links:
    - php


php:
  image: php:7-fpm
  volumes:
    - ./www:/www
